import { useCallback } from 'react'
import { useMappedState } from 'redux-react-hook'

export const useHook = () => {

    const { } = useMappedState(useCallback(state => {}, []))

    return {

    }
}