import React from 'react'
import css from './Spinner.module.css'

const Spinner = () => {
    return <span className={css.content}>
        <b>I'm the spinner...</b>
    </span>
}

export default Spinner